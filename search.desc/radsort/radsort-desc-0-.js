searchState.loadedDescShard("radsort", 0, "<code>radsort</code> is a radix sort implementation for sorting by …\nTypes which can be used as sorting keys.\nSorts the slice.\nSorts the slice indirectly, using a key extraction …\nSorts the slice using a key extraction function.\nSorting functions which don’t use optimizations based on …\nVersion of <code>sort</code> which does not skip digits (bytes).\nVersion of <code>sort_by_cached_key</code> which does not skip digits …\nVersion of <code>sort_by_key</code> which does not skip digits (bytes).")